# Published assets

Store images, diagrams, and other rendered assets directly under `docs/` beside the Markdown pages that use them. This `guides/assets/` directory contains authoring guidance and is not synchronized by MSP Portal.

Prefer descriptive filenames and keep asset references relative so they work on GitHub and after MSP Portal sync.
