# Components

Use component content for reusable building blocks, modules, or feature areas.

Choose this approach when readers need to understand a named part of the system and how it is used by other parts. Put the resulting reader-facing page directly under `docs/`.
