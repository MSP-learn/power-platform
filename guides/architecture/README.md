# Architecture

Use architecture content for system boundaries, major decisions, diagrams, and lifecycle overviews.

Choose this approach when readers need to understand how the documented area is shaped before they change or extend it. Put the resulting reader-facing page directly under `docs/`.
