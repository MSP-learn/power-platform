# Snippets

Use snippet content for small reusable fragments such as command sequences, short code blocks, or markup examples.

If a snippet needs substantial explanation, create a separate reader-facing page under `docs/` and connect the two pages through descriptive links.
